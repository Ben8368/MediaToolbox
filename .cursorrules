# MediaToolbox — Cursor 入口

> 自动加载摘要。完整规则与冲突裁决以 `AGENTS.md` 为准。

## 开局

1. 每轮必读 `CONTEXT.md` 和 `LESSONS.md`。
2. 代码改动按需读 `docs/ARCHITECTURE.md`、`docs/AI_RULES.md`、`docs/FRONTEND_API_CONTRACT.md` 和 `docs/UI_COMPAT.md`。
3. 高风险、素材包或发布任务回到 `AGENTS.md` 的四层读取结构。

## 只记三件事

- 审查、验证、提交和推送全部按 `docs/AI_RULES.md`，本文件不复制分支策略。
- 本仓库是 npm workspaces TypeScript monorepo；默认客观验证为 `npm run verify`。
- 不确定时回到 `AGENTS.md`，不要在本摘要里新增规则。
