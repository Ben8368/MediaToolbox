# 已偿还技术债归档：2026-07

> 从 `docs/TECH_DEBT.md` 迁移；活跃债务请只查看原文。

## 2026-07-15

- TD-029：Web Composer 预设页 logo/icon slot PNG 替换失败。已新增 `replaceSlotWithImage`，上传图片会同时更新 image 候选并切换 `activeKind: 'image'`；Lumora 与 VaultShield 场景已补回归覆盖。
- TD-027：所有服务端 Job ID 与 PSD workorder ID 改用带业务前缀的 UUID；同一冻结时钟下并发创建 100 个任务均成功且 ID 唯一。
- Release matrix 并发发布风险已修复：三平台仅构建、烟测和上传 workflow artifact，单一 `publish` job 汇总并发布，已发布 tag 禁止覆盖。
- TD-012：纯 Web 模式下浏览器 app 明确 Electron 会话边界，并提供“打开下载器”的替代路径。
- TD-022：移除了没有执行路径的 `JobStatus.retrying` 与相关状态转移、指标过滤和前端展示。
- TD-024：下载请求收敛为共享 `FetchTaskDraft`，输出目录、H.264/转码、Cookie 与有界批次并发映射到调度器或 yt-dlp 参数；已补 API 与 downloader 回归测试。
- TD-025：Job 运行中字段更新改为 `patchIfStatus(..., 'running')` 原子持久化，Browser Network 进度回归确认状态保持 `running`。
- TD-026：Job 终态写入改为数据库 compare-and-set；取消后到达的完成事件保持 `canceled` 且不生成 Asset。

## 2026-07-08

- TD-016：Apple Silicon 的 `ioreg IOAccelerator` GPU 利用率采集已验收；Intel Mac 保持“未检测到可用计数器”降级文案。

## 2026-07-07

- TD-001：PowerShell 空输出误报 GPU 可用，已补空输出解析保护与测试。
- TD-002：`parseDataRateText` 已支持 `~4.20MiB/s` 等预估速率格式。
- TD-003：`parseDataRateText` 改为枚举合法单位，拒绝 `IB/s`。
- TD-004：`sampleGpu` 加入 in-flight Promise 缓存，消除缓存击穿竞态。
- TD-005：`parseDataRateText` 改为 `number | null`，解析失败不再写入假 0。
- TD-006：`buildMetrics` 改为并发采样内存与 GPU。
- TD-007：`sampleProjectNetworkRates` 合并为单次 reduce。
- TD-008：`formatBytesPerSecond` 新增 GB/s 显示。
- TD-009：`task.state` 只在速率变化时写入，解析失败时清理速率字段。
- TD-017：前后端 app ID 契约已对齐 `packages/contracts` 与前端 registry。
- TD-018：PSD manifest/slot/render input 类型收敛到 `packages/contracts`，非文字 slot 明确拒绝。
- TD-010：`formatBytesPerSecond` 与 `formatSpeed` 合并为共享实现。
- TD-011：CPU 采样状态封装为 `createCpuSampler()` 闭包。
- TD-014：Electron production runtime bundle、API 子进程链路和 macOS arm64 API health 烟测已接入；后续 renderer 真机验收迁移为 TD-019。
